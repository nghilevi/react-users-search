describe('utils', () => {

    // DISPLAYING
    describe('filterList', () => {

        it('should return an empty list if passed itemsList is NOT an array', () => {

        })

        // more tests should also be added for other cases e.g filteredKey is string or not and so on, so more checks should also be implemented

        it('should return a new list filtered by key and value', () => {

        })

    })

})